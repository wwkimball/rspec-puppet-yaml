require "spec_helper"

RSpec.describe Rspec::Puppet::Yaml do
  it "has a version number" do
    expect(Rspec::Puppet::Yaml::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
